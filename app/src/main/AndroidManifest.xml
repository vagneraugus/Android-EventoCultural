<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jose.eventocultural">

    <application
        android:allowBackup="true"
        android:icon="@drawable/calendar1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BuscaCadActivity" />
        <activity android:name=".BuscarQtdePessoas" />
        <activity android:name=".BuscarSujestoesActivity" />
        <activity android:name=".AlterarActivity"></activity>
    </application>

</manifest>